{
  "name": "robot",
  "version": "1.0.0",
  "description": "We have installed a robot in our warehouse and now we need to be able to send commands to control it. We need you to implement the primary control mechanism.",
  "main": "the warehouse then the following commands will move it to the middle of the warehouse.",
  "scripts": {
    "local": "npx ts-node src/app.ts",
    "test": "npx jest"
  },
  "jest":{
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.6",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
